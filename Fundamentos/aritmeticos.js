const [a, b, c, d] = [3, 4, 6, 4]
const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = c * d
const divisao = (d / a).toFixed(2)
const modulo = c % 2
const divisao = (c / a).toFixed(2)
console.log(soma, subtracao, multiplicacao, divisao, modulo)
